{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://stemma.network/schema/model.schema.json",
  "title": "Stemma Model Record",
  "type": "object",
  "$defs": {
    "sourced_string": {
      "type": "object",
      "properties": {
        "value": { "type": ["string", "null"] },
        "source": { "type": ["string", "null"], "format": "uri" },
        "status": { "enum": ["recorded", "partial", "not_recorded"] },
        "note": { "type": "string", "description": "Caveat that must travel with this value." }
      },
      "required": ["value", "status"],
      "additionalProperties": false
    },
    "sourced_number": {
      "type": "object",
      "properties": {
        "value": { "type": ["number", "null"] },
        "source": { "type": ["string", "null"], "format": "uri" },
        "status": { "enum": ["recorded", "partial", "not_recorded"] },
        "note": { "type": "string", "description": "Caveat that must travel with this value." }
      },
      "required": ["value", "status"],
      "additionalProperties": false
    },
    "availability": {
      "type": "object",
      "description": "Whether the artifact itself (weights, or dataset files) could be obtained, as checked on a given date. Not a claim about API access.",
      "properties": {
        "value": { "enum": ["available", "partial", "gated", "removed", "never_released", "unknown"] },
        "checked": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
        "source": { "type": "string", "format": "uri" },
        "note": { "type": "string" }
      },
      "required": ["value", "checked", "source"],
      "additionalProperties": false
    }
  },
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable, never-reused identifier. Lowercase, hyphenated.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "name": { "type": "string" },
    "identifiers": {
      "type": "object",
      "description": "External ids for matching this record to other catalogs. Hugging Face repo ids are listed so ingest can recognize a repo already in Stemma; aliases (renamed repos) are allowed.",
      "properties": {
        "huggingface": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": false
    },
    "developer": { "type": "string" },
    "release_date": { "$ref": "#/$defs/sourced_string" },
    "weights_status": { "enum": ["open", "api_only", "closed", "unknown"] },
    "availability": { "$ref": "#/$defs/availability" },
    "license": { "$ref": "#/$defs/sourced_string" },
    "architecture": {
      "type": "object",
      "properties": {
        "family": { "type": "string" },
        "n_layers": { "$ref": "#/$defs/sourced_number" },
        "hidden_size": { "$ref": "#/$defs/sourced_number" },
        "n_heads": { "$ref": "#/$defs/sourced_number" },
        "vocab_size": { "$ref": "#/$defs/sourced_number" },
        "positional_encoding": { "$ref": "#/$defs/sourced_string" }
      },
      "required": ["family"],
      "additionalProperties": true
    },
    "training_data": { "$ref": "#/$defs/sourced_string" },
    "techniques": {
      "type": "array",
      "items": { "type": "string" }
    },
    "primary_sources": {
      "type": "array",
      "items": { "type": "string", "format": "uri" },
      "minItems": 1
    },
    "record_history": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "date": { "type": "string", "format": "date" },
          "change": { "type": "string" },
          "by": { "type": "string" }
        },
        "required": ["date", "change", "by"],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "id",
    "name",
    "developer",
    "release_date",
    "weights_status",
    "availability",
    "license",
    "architecture",
    "training_data",
    "primary_sources",
    "record_history"
  ],
  "additionalProperties": false
}
